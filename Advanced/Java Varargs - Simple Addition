class Add{
    void add(int ...a){
        int sum=0;
       
        for(int i=0;i<a.length;i++){
            System.out.print(a[i]);
            System.out.print(i==a.length-1 ? "=" : "+");
            sum+=a[i];
        }
        System.out.println(sum);
    }
}
